[package]
name = "rustree"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde = { version = "1.0.193", features = ["derive"] }

[workspace]
members = ["gui"]
